<?xml version="1.0" ?>
<SmellBaseline>
  <ManuallySuppressedIssues/>
  <CurrentIssues>
    <ID>BlankLineBetweenWhenConditions:SearchScreen.kt:when (this) { is AddPodcastResult.Added -&gt; { val noun = if (podcast.source == PodcastSource.YOUTUBE) "videos" else "episodes" "Added ${podcast.title} · $episodeCount $noun" } is AddPodcastResult.AlreadyInLibrary -&gt; "${podcast.title} is already in your library" is AddPodcastResult.NoFeedAvailable -&gt; "$title has no RSS feed and can't be downloaded" AddPodcastResult.NotFound -&gt; "Apple doesn't know that podcast id" AddPodcastResult.InvalidInput -&gt; "That doesn't look like a podcast link" // Almost always a single video pasted instead of the playlist, so the message says what to do // rather than just what went wrong. AddPodcastResult.NotAPlaylist -&gt; "That's a YouTube link, but not a playlist — open the playlist and copy its link" is AddPodcastResult.Failed -&gt; "Couldn't read that feed: ${cause.message ?: "unknown error"}" }</ID>
    <ID>BlankLineBetweenWhenConditions:SearchScreen.kt:when { isAdding -&gt; CircularProgressIndicator(modifier = Modifier.size(20.dp)) addable -&gt; Icon( imageVector = Icons.Rounded.Add, contentDescription = "Add ${result.title}", ) }</ID>
  </CurrentIssues>
</SmellBaseline>
