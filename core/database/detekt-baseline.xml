<?xml version="1.0" ?>
<SmellBaseline>
  <ManuallySuppressedIssues/>
  <CurrentIssues>
    <ID>ImportOrdering:DatabaseModule.kt:import android.content.Context import androidx.room.Room import dagger.Module import dagger.Provides import dagger.hilt.InstallIn import dagger.hilt.android.qualifiers.ApplicationContext import dagger.hilt.components.SingletonComponent import javax.inject.Singleton import md.borisveriga.bpodcat.core.database.BPodcatDatabase import md.borisveriga.bpodcat.core.database.dao.EpisodeDao import md.borisveriga.bpodcat.core.database.dao.PodcastDao import md.borisveriga.bpodcat.core.database.dao.QueueDao import md.borisveriga.bpodcat.core.database.migration.MIGRATION_1_2 import md.borisveriga.bpodcat.core.database.migration.MIGRATION_2_3</ID>
    <ID>ImportOrdering:EpisodeDaoTest.kt:import androidx.room.Room import androidx.test.core.app.ApplicationProvider import java.time.Instant import kotlinx.coroutines.flow.first import kotlinx.coroutines.test.runTest import md.borisveriga.bpodcat.core.database.BPodcatDatabase import md.borisveriga.bpodcat.core.database.model.EpisodeEntity import md.borisveriga.bpodcat.core.database.model.PodcastEntity import md.borisveriga.bpodcat.core.model.DownloadState import org.junit.After import org.junit.Assert.assertEquals import org.junit.Assert.assertNull import org.junit.Assert.assertTrue import org.junit.Before import org.junit.Test import org.junit.runner.RunWith import org.robolectric.RobolectricTestRunner import org.robolectric.annotation.Config</ID>
    <ID>ImportOrdering:EpisodeEntity.kt:import androidx.room.ColumnInfo import androidx.room.Entity import androidx.room.ForeignKey import androidx.room.Index import androidx.room.PrimaryKey import java.time.Instant import md.borisveriga.bpodcat.core.model.DownloadState import md.borisveriga.bpodcat.core.model.Episode</ID>
    <ID>ImportOrdering:PodcastEntity.kt:import androidx.room.ColumnInfo import androidx.room.Embedded import androidx.room.Entity import androidx.room.Index import androidx.room.PrimaryKey import java.time.Instant import md.borisveriga.bpodcat.core.model.Podcast import md.borisveriga.bpodcat.core.model.PodcastSource import md.borisveriga.bpodcat.core.model.PodcastWithCounts</ID>
    <ID>NoBlankLineBeforeRbrace:EpisodeDao.kt:EpisodeDao$ </ID>
    <ID>TopLevelPropertyNaming:Migrations.kt:/** * Adds `podcasts.source`, which records whether a show is an RSS feed or a YouTube playlist. * * Everything already in a v1 library predates YouTube support and is therefore an RSS feed, so the * column default classifies every existing row correctly and no data has to be inspected or * rewritten. * * The `DEFAULT 'RSS'` must stay in step with `@ColumnInfo(defaultValue = "RSS")` on * [md.borisveriga.bpodcat.core.database.model.PodcastEntity]: Room compares the migrated schema * against the entity definition at open time and refuses to start if they disagree. */ val MIGRATION_1_2: Migration = object : Migration(1, 2) { override fun migrate(connection: SQLiteConnection) { connection.execSQL("ALTER TABLE podcasts ADD COLUMN source TEXT NOT NULL DEFAULT 'RSS'") } }</ID>
    <ID>TopLevelPropertyNaming:Migrations.kt:/** * Deletes episodes whose `audio_url` is not one the player may resolve. * * The scheme allowlist (`isPlayableMediaUrl` in `:core:model`) landed with the parsers that write * this table, but a library populated before it existed can still hold a `file:` or `content:` URL * that a hostile feed published. Those rows are not repairable — there is no correct audio URL to * put back — so they are removed. * * The `queue` entries are deleted explicitly rather than left to the `ON DELETE CASCADE` on * `queue.episode_id`. Room runs migrations with `PRAGMA foreign_keys = OFF` — it has to, because the * usual table-rebuild migration would otherwise cascade away every child row — so inside a migration * a cascade that works perfectly at runtime silently does nothing, and the deleted episodes would * leave queue rows the player could never resolve. * * Purely a data migration: the schema of version 3 is identical to version 2, and the version bump * exists only so that the sweep runs exactly once per install rather than on every open. * * ## Keeping this in step with the Kotlin guard * * The predicate is duplicated here in SQL because Room migrations cannot call Kotlin. It is the * looser of the two on purpose — it keeps every `http`/`https`/sentinel row and deletes everything * else, so it can never delete a row `isPlayableMediaUrl` would accept. It does keep a few rows that * the Kotlin guard would reject (an over-long URL, one containing whitespace, a sentinel with a * malformed video id); `toMediaItemOrNull` refuses those at the point of use, which is why that * second gate exists. * * `LIKE` is ASCII-case-insensitive in SQLite by default, which matches the case-insensitive scheme * comparison in Kotlin. None of the patterns contain `_` or `%`, so no escaping is needed. */ val MIGRATION_2_3: Migration = object : Migration(2, 3) { override fun migrate(connection: SQLiteConnection) { connection.execSQL( """ DELETE FROM episodes WHERE audio_url NOT LIKE 'http://%' AND audio_url NOT LIKE 'https://%' AND audio_url NOT LIKE 'youtube://video/%' """.trimIndent(), ) // Stated as "anything with no episode" rather than by repeating the URL predicate, so it // stays correct no matter what the statement above deletes. connection.execSQL("DELETE FROM queue WHERE episode_id NOT IN (SELECT id FROM episodes)") } }</ID>
  </CurrentIssues>
</SmellBaseline>
