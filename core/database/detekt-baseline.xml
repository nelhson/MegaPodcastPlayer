<?xml version="1.0" ?>
<SmellBaseline>
  <ManuallySuppressedIssues/>
  <CurrentIssues>
    <ID>NoBlankLineBeforeRbrace:EpisodeDao.kt:EpisodeDao$ </ID>
    <ID>TopLevelPropertyNaming:Migrations.kt:/** * Adds `podcasts.is_pinned` and `episodes.is_hidden`, the two flags behind the new row gestures. * * `is_pinned` is the library's coarse ordering: a pinned show sorts above every unpinned one, and * `sort_order` still decides within each group. Nothing is seeded, because nothing was pinned * before this version existed and `0` says exactly that. * * `is_hidden` is a tombstone rather than a deletion. An episode id is derived from the feed's GUID, * so deleting a row only tells the next refresh to insert it again; flagging it keeps the row * around for `insertIgnoringExisting` to recognise and skip, which is what makes "remove from the * list" stick for an RSS show. Every query that backs a list filters on it. Again nothing is * seeded: no episode was dismissed before the gesture existed. * * Both `DEFAULT 0`s must stay in step with `@ColumnInfo(defaultValue = "0")` on * [md.borisveriga.bpodcat.core.database.model.PodcastEntity] and * [md.borisveriga.bpodcat.core.database.model.EpisodeEntity]: Room compares the migrated schema * against the entity definitions at open time and refuses to start if they disagree. * * Purely additive — no row is read or rewritten, so the migration is instant on any library size. */ val MIGRATION_5_6: Migration = object : Migration(5, 6) { override fun migrate(connection: SQLiteConnection) { connection.execSQL("ALTER TABLE podcasts ADD COLUMN is_pinned INTEGER NOT NULL DEFAULT 0") connection.execSQL("ALTER TABLE episodes ADD COLUMN is_hidden INTEGER NOT NULL DEFAULT 0") } }</ID>
    <ID>TopLevelPropertyNaming:Migrations.kt:/** * Adds `podcasts.source`, which records whether a show is an RSS feed or a YouTube playlist. * * Everything already in a v1 library predates YouTube support and is therefore an RSS feed, so the * column default classifies every existing row correctly and no data has to be inspected or * rewritten. * * The `DEFAULT 'RSS'` must stay in step with `@ColumnInfo(defaultValue = "RSS")` on * [md.borisveriga.bpodcat.core.database.model.PodcastEntity]: Room compares the migrated schema * against the entity definition at open time and refuses to start if they disagree. */ val MIGRATION_1_2: Migration = object : Migration(1, 2) { override fun migrate(connection: SQLiteConnection) { connection.execSQL("ALTER TABLE podcasts ADD COLUMN source TEXT NOT NULL DEFAULT 'RSS'") } }</ID>
    <ID>TopLevelPropertyNaming:Migrations.kt:/** * Adds `sort_order` to `podcasts` and `episodes`, which is what makes both hand-orderable. * * Both columns are seeded from the ordering the screens already showed, so the upgrade is * invisible: nothing moves until the user drags something. Seeding matters more than it looks — * leaving every row at the default `0` would collapse the library into one arbitrary blob the * first time anything was reordered. * * The library is seeded from its alphabetical rank, which is what `PodcastDao` sorted by until * now. Ties on title are broken by `id` so the seed is deterministic rather than dependent on the * order SQLite happens to scan in. * * Episodes are seeded only for YouTube-sourced shows, since they are the only ones the user can * reorder; the rank is by date, newest first, matching what the show's screen displayed. Undated * episodes sort last, exactly as `observeByPodcast` puts them. * * Both `DEFAULT 0`s must stay in step with `@ColumnInfo(defaultValue = "0")` on * [md.borisveriga.bpodcat.core.database.model.PodcastEntity] and * [md.borisveriga.bpodcat.core.database.model.EpisodeEntity]: Room compares the migrated schema * against the entity definitions at open time and refuses to start if they disagree. */ val MIGRATION_4_5: Migration = object : Migration(4, 5) { override fun migrate(connection: SQLiteConnection) { connection.execSQL("ALTER TABLE podcasts ADD COLUMN sort_order INTEGER NOT NULL DEFAULT 0") connection.execSQL("ALTER TABLE episodes ADD COLUMN sort_order INTEGER NOT NULL DEFAULT 0") // A correlated count is the rank: how many rows sort before this one. Cheap enough at // library sizes, and it avoids needing window functions on older SQLite builds. connection.execSQL( """ UPDATE podcasts SET sort_order = ( SELECT COUNT(*) FROM podcasts other WHERE other.title COLLATE NOCASE &lt; podcasts.title COLLATE NOCASE OR (other.title COLLATE NOCASE = podcasts.title COLLATE NOCASE AND other.id &lt; podcasts.id) ) """.trimIndent(), ) connection.execSQL( """ UPDATE episodes SET sort_order = ( SELECT COUNT(*) FROM episodes other WHERE other.podcast_id = episodes.podcast_id AND ( (other.published_at IS NOT NULL AND episodes.published_at IS NULL) OR (other.published_at &gt; episodes.published_at) OR (other.published_at = episodes.published_at AND other.id &lt; episodes.id) OR (other.published_at IS NULL AND episodes.published_at IS NULL AND other.id &lt; episodes.id) ) ) WHERE podcast_id IN (SELECT id FROM podcasts WHERE source = 'YOUTUBE') """.trimIndent(), ) } }</ID>
    <ID>TopLevelPropertyNaming:Migrations.kt:/** * Adds an index on `episodes.published_at` for the Latest feed. * * The feed orders every episode in the library by date, across shows. The existing composite index * leads with `podcast_id`, so it cannot serve that sort — without this index the query degrades to * a full scan plus a sort of the whole table on every emission, and the feed re-queries whenever * any episode row changes. * * The index name must be exactly what Room generates for * `Index(value = ["published_at"])` on `EpisodeEntity`: Room validates the migrated schema against * the entity definition when it opens the database and refuses to start if a name differs. * * Purely additive — no data is read or rewritten, so the migration is instant on any library size. */ val MIGRATION_3_4: Migration = object : Migration(3, 4) { override fun migrate(connection: SQLiteConnection) { connection.execSQL( "CREATE INDEX IF NOT EXISTS index_episodes_published_at ON episodes (published_at)", ) } }</ID>
    <ID>TopLevelPropertyNaming:Migrations.kt:/** * Deletes episodes whose `audio_url` is not one the player may resolve. * * The scheme allowlist (`isPlayableMediaUrl` in `:core:model`) landed with the parsers that write * this table, but a library populated before it existed can still hold a `file:` or `content:` URL * that a hostile feed published. Those rows are not repairable — there is no correct audio URL to * put back — so they are removed. * * The `queue` entries are deleted explicitly rather than left to the `ON DELETE CASCADE` on * `queue.episode_id`. Room runs migrations with `PRAGMA foreign_keys = OFF` — it has to, because the * usual table-rebuild migration would otherwise cascade away every child row — so inside a migration * a cascade that works perfectly at runtime silently does nothing, and the deleted episodes would * leave queue rows the player could never resolve. * * Purely a data migration: the schema of version 3 is identical to version 2, and the version bump * exists only so that the sweep runs exactly once per install rather than on every open. * * ## Keeping this in step with the Kotlin guard * * The predicate is duplicated here in SQL because Room migrations cannot call Kotlin. It is the * looser of the two on purpose — it keeps every `http`/`https`/sentinel row and deletes everything * else, so it can never delete a row `isPlayableMediaUrl` would accept. It does keep a few rows that * the Kotlin guard would reject (an over-long URL, one containing whitespace, a sentinel with a * malformed video id); `toMediaItemOrNull` refuses those at the point of use, which is why that * second gate exists. * * `LIKE` is ASCII-case-insensitive in SQLite by default, which matches the case-insensitive scheme * comparison in Kotlin. None of the patterns contain `_` or `%`, so no escaping is needed. */ val MIGRATION_2_3: Migration = object : Migration(2, 3) { override fun migrate(connection: SQLiteConnection) { connection.execSQL( """ DELETE FROM episodes WHERE audio_url NOT LIKE 'http://%' AND audio_url NOT LIKE 'https://%' AND audio_url NOT LIKE 'youtube://video/%' """.trimIndent(), ) // Stated as "anything with no episode" rather than by repeating the URL predicate, so it // stays correct no matter what the statement above deletes. connection.execSQL("DELETE FROM queue WHERE episode_id NOT IN (SELECT id FROM episodes)") } }</ID>
  </CurrentIssues>
</SmellBaseline>
