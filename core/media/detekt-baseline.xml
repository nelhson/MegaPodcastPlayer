<?xml version="1.0" ?>
<SmellBaseline>
  <ManuallySuppressedIssues/>
  <CurrentIssues>
    <ID>SpacingBetweenDeclarationsWithComments:DownloadModule.kt:DownloadModule$/** * The network end of both data source chains. * * [ResolvingDataSource] wraps OkHttp rather than replacing it, so a `youtube://video/&lt;id&gt;` URI * becomes a real audio URL immediately before the request goes out, while every ordinary * podcast URL passes through untouched. * * Crucially this factory is only ever used as the *upstream* of a cache — never as the whole * chain. Both callers below put a [CacheDataSource] above it, which is what keeps cache keys * and download ids anchored to the sentinel instead of to a URL that expires within hours. */</ID>
  </CurrentIssues>
</SmellBaseline>
